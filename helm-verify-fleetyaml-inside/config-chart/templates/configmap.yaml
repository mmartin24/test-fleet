apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config-new
data:
  test: "value"
  name: {{ .Values.name }}
  url: {{ .Values.url | quote }}
---
{{- if .Values.otherConfigMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: other-config-map-new
data:
  test: "value"
  name: {{ .Values.name }}
  url: {{ .Values.url | quote }}
{{- end }}
